# Dependências de teste — incluir no CI/CD
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
aiosqlite==0.20.0      # SQLite async para testes sem Docker/PostgreSQL
greenlet>=3.0.0        # Rastreamento de cobertura em código assíncrono (asyncio + aiosqlite)
